{{app-container
    model=model
    arrangeBy=arrangeBy
    sortBy=sortBy
    filterBy=filterBy
    searchQuery=query
    setSearchQuery=(action "setSearchQuery")
    showShortcutBar=(action "showShortcutBar")
    toggleShortcutBar=(action "toggleShortcutBar")
}}

{{shortcut-bar
    isOpen=isShortcutBarOpen
    close=(action "toggleShortcutBar")
}}
