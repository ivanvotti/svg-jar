{{app-navbar
    arrangeDropdownItems=model.arrangeBy
    sortDropdownItems=model.sortBy
}}

<div class="c-app-main">
  {{#if model.filters}}
    {{app-sidebar sidebarFilters=model.filters}}
  {{/if}}

  <div class="c-app-content">
    {{#if arrangeBy}}
      {{asset-groups
          assets=assets
          currentAsset=currentAsset
          arrangeBy=arrangeBy
          setCurrentAsset=(action "setCurrentAsset")
      }}
    {{else}}
      {{asset-list
          assets=assets
          currentAsset=currentAsset
          setCurrentAsset=(action "setCurrentAsset")
      }}
    {{/if}}
  </div>

  {{app-pane currentAsset=currentAsset}}
</div>
