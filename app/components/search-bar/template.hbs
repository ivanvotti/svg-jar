{{svg-jar "search" class="c-search-bar__icon"}}

{{input
  class="c-search-bar__input js-search-bar-input"
  placeholder="Search assets..."
  value=@searchQuery
  enter=(action "onEnter")
  escape-press=(action "onEscape")
}}

{{#if @searchQuery}}
  <button class="c-search-bar__reset" {{action "resetQuery"}}>
    ✕
  </button>
{{/if}}
